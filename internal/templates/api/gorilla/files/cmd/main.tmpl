package main

import (
	"{{.Name}}/cmd/server"
	"{{.Name}}/configs"
	"{{.Name}}/internal/adapters/handlers/gorilla"
	"{{.Name}}/internal/adapters/repositories/mock"
	"{{.Name}}/internal/core/services"
)

func init() {
	configs.LoadEnv()
}

func main() {
	db := &mock.MockRepository{}
	service := services.NewUserService(db)
	handlers := gorilla.NewRouter(service)
	server.Serve(handlers)
}
